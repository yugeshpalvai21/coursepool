<div class="container">
  <div class="card">
    <div class="card-header text-center display-5">New Lesson Details</div>
    <div class="card-body">
        <%= render 'form', lesson: @lesson, course: @course %>
    </div>
    <div class="card-footer">
      <%= link_to 'Back', course_path(@course) %>
    </div>
  </div>
</div>
